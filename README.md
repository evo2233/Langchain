This is a repo about Langchain learning, I will follow some online tutorials to try some relevant techs that I fancy and might be useful for my career as a bonus.

In the `sematic` folder we will learn some basic usage of sematic search.

In the `agent` folder we can see how to build an agent with tools and memory by LangChain.

In the `RAG` folder we will build a system that can use docs in Lark(飞书) and code

# env
python 3.11\
langchain (1.0.X)\
langchain-community\
ollama\
langchain-ollama\
chromadb\
langchain-chroma\
psycopg\
langgraph-checkpoint-postgres\
pypdf

# model
nomic-embed-text (ollama)\
deepseek (key)
